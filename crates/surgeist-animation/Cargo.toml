[package]
name = "surgeist-animation"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/bj-data-eng/surgeist"
readme = "README.md"
description = "CSS animation and transition timing contracts for Surgeist."

[lib]
name = "surgeist_animation"
path = "src/lib.rs"

[dev-dependencies]
stats_alloc = { version = "=0.1.10", default-features = false }

[[bench]]
name = "sampling"
harness = false
